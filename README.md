# KilauCMS

## Autentikasi Lokal
- Login menggunakan email dan password dari tabel `users`.
- Registrasi user baru tersedia di halaman `/register`.
- Role admin ditentukan dari kolom `role` bernilai `admin`.
- Kolom `sso_sub` tetap dipakai sebagai ID legacy jika data lama masih terhubung ke SSO.
